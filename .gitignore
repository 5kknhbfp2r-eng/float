# --- secrets: NEVER commit ---
capi.txt
sapi.txt
**/capi.txt
**/sapi.txt
mapi.txt
dbapi.txt
.env
*.env
**/.env

# --- large / reproducible caches (re-fetchable from EDGAR; kept on disk, not in git) ---
engine/data/_cache/filing_text/
engine/data/_cache/edgar_submissions/
engine/data/_cache/scans/
engine/data/_cache/dossiers/
engine/data/_cache/xbrl/
engine/data/_cache/_13f_lookup.json
engine/data/_cache/company_tickers.json
engine/data/_cache/secapi_ticker_cik.json
engine/data/*.parquet
# NOTE: engine/data/_cache/receipts/ and manifests/ ARE committed (derivation audit + reuse layer)

# --- python ---
__pycache__/
*.pyc
*.pyo

# --- bulk data / build artifacts / logs ---
*.parquet
*.pkl
*.log
node_modules/

# --- recipe-replay simulation outputs (regenerable via engine/sim_replay.py) ---
_sim_replay.csv
_sim_recipes.json
_emit_worklist.json

# --- agent scratch / stray working files ---
tmp_*
*.jpg
*.tmp
claude.txt
*.output

# wave manifest (regenerable via emit_worklist)
_emit_wave_manifest.json

# raw workflow emit output (merged into _recipes_emitted.json)
_wave_emitted_raw.json

# label-QC raw verdicts (regenerable from the workflow output)
_labelqc_raw.json

# O/S audit output (regenerable via audit_os.py)
_audit_os.csv
